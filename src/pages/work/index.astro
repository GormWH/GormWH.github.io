---
import MetaRefresh from "@components/MetaRefresh.astro";
import { DEFAULT_LOCALE, localizedPath } from "@lib/i18n";

const path = localizedPath(DEFAULT_LOCALE, 'work');
---

<MetaRefresh path={path} />
